diff --git a/st.c b/st.c
index 64bc208..ddffd2f 100644
--- a/st.c
+++ b/st.c
@@ -1743,7 +1743,7 @@ kscrolldown(const Arg* a)
 	int n = a->i;
 
 	if (n < 0)
-		n = term.row + n;
+		n = term.row + n + 1;
 
 	if (n > term.scr)
 		n = term.scr;
@@ -1761,7 +1761,7 @@ kscrollup(const Arg* a)
 	int n = a->i;
 
 	if (n < 0)
-		n = term.row + n;
+		n = term.row + n + 1;
 
 	if (term.scr <= histsize - n) {
 		term.scr += n;
