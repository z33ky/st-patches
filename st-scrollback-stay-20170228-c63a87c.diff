diff --git a/st.c b/st.c
index 64bc208..cd31620 100644
--- a/st.c
+++ b/st.c
@@ -1521,9 +1521,6 @@ ttyread(void)
 	if (buflen > 0)
 		memmove(buf, ptr, buflen);
 
-	if (term.scr > 0 && term.scr < histsize-1)
-		term.scr++;
-
 	return ret;
 }
 
@@ -1860,6 +1857,8 @@ tnewline(int first_col)
 	int y = term.c.y;
 
 	if (y == term.bot) {
+		if (BETWEEN(term.scr, 1, term.histlen - 1))
+			++term.scr;
 		tscrollup(term.top, 1, 1);
 	} else {
 		y++;
